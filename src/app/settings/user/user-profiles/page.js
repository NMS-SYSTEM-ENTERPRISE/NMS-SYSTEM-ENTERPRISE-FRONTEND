import UserProfiles from '@/screens/settings/user/user-profiles';

export const metadata = {
  title: 'User Profiles | NMS-SYSTEM-ENTERPRISE',
  description: 'Manage user profiles and access control scopes',
};

export default function UserProfilesPage() {
  return <UserProfiles />;
}
